import React, { memo } from 'react';

function CodeInterpreter({ conversationId }: { conversationId?: string | null }) {
  // Return null to disable the component and make it invisible
  return null;
}

export default memo(CodeInterpreter);
